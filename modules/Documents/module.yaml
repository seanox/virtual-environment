module:
    description: >
        The quick function opens the Documents directory of the virtual
        environment.

    depends: Platform

    meta:
        tab:       1
        button:    52
        text:      Documents
        icon:      %windir%\system32\shell32.dll,126
        execute:   %windir%\explorer.exe
        arguments: #[module.environment]