module:
    description: >
        Command line for the Virtual Development Environment. It uses the
        environment variables from the virtual environment, including the PATH,
        and is therefore different from the Windows command line because it
        does not know the variables and path.
        In the module directory further extensions, scripts and tools can be
        added, which can then be used directly via the PATH.

    prepare:
        - #[module.destination]\console.cmd

    commons: |
        SET PATH=#[module.environment];%PATH%

    control: |
        <Button>
          <Tab_ID>1</Tab_ID>
          <ID>44</ID>
          <Text>Console</Text>
          <IconFile>%windir%\System32\cmd.exe</IconFile>
          <IconIndex>0</IconIndex>
          <Filename>#[module.environment]\console.cmd</Filename>
          <Arguments/>
          <WorkingDirectory>#[module.environment]</WorkingDirectory>
          <WindowStyle>0</WindowStyle>
          <WindowStyleEx>0</WindowStyleEx>
          <RunAsAdmin>false</RunAsAdmin>
          <WindowTitle/>
          <WindowProcessName/>
          <WindowRect>0,0,0,0</WindowRect>
          <GroupID/>
        </Button>